export type UserViewModel = {
    id: Number, 
    login: string,
    email: string,
    passwordHash: string,
    isAdmin: boolean
}

export type UserInfoModel = {
    id: Number, 
    login: string,
    email: string,
    isAdmin: boolean
}