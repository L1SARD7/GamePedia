export type UserViewModel = {
    id: number;
    login: string;
    email: string;
    passwordHash: string;
    isAdmin: boolean;
};

export type UserInfoModel = {
    id: number;
    username: string;
    email: string;
    isAdmin: boolean;
};
