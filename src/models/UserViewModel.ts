export type UserViewModel = {
    id: string;
    login: string;
    email: string;
    passwordHash: string;
    isAdmin: boolean;
};

export type UserInfoModel = {
    id: string;
    username: string;
    email: string;
    isAdmin: boolean;
};
